'use strict';

/**
 * @ngdoc function
 * @name initApp.controller:GateDetailCtrl
 * @description
 * # GateDetailCtrl
 * Controller of the initApp
 */
angular.module('initApp')
  .controller('GateDetailCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
